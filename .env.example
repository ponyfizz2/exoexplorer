# No environment variables are required — the archive proxy needs no API key.
# Optional: override the NASA TAP endpoint (a mirror, or a test double).
# EXOPLANET_TAP_URL=https://exoplanetarchive.ipac.caltech.edu/TAP/sync
